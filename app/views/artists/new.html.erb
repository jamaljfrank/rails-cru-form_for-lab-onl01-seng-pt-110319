<h1>Artist Form</h1>

<%= form_for(@artist) do |f| %>

  <%= f.label :name %><br>
  <%= f.text_field @artist.name %><br>

  <%= f.label :bio %><br>
  <%= f.text_area @artist.bio %><br>

  <%= f.submit %>

<% end %>